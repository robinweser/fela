export interface Theme {
  color: {
    primary: string
    secondary: string
    additional: string
  }
}

export const theme: Theme = {
  color: {
    primary: 'lightblue',
    secondary: 'orange',
    additional: 'lightgreen'
  }
};